"use client";
import { useState } from "react";
export default function ForgotPassword(){
 const [email,setEmail]=useState(""),[busy,setBusy]=useState(false),[message,setMessage]=useState(""),[error,setError]=useState("");
 async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setMessage("");setError("");const r=await fetch("/api/auth/forgot-password",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email})});const d=await r.json();if(!r.ok)setError(d.error||"Aanvraag mislukt.");else setMessage("Als er een RankFix-account met dit e-mailadres bestaat, hebben we een resetlink gestuurd. Controleer ook je spam.");setBusy(false);}
 return <main className="min-h-screen bg-[#0B1220] px-5 py-12 text-white"><div className="mx-auto max-w-md"><a href="/account" className="text-lg font-bold">RankFix <span className="text-emerald-300">AI</span></a><div className="mt-10 rounded-3xl border border-white/10 bg-white/[0.04] p-7"><h1 className="text-3xl font-black">Wachtwoord vergeten?</h1><p className="mt-2 text-sm text-slate-400">Vul je e-mailadres in. We sturen een veilige resetlink.</p><form onSubmit={submit} className="mt-7 space-y-4"><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="E-mailadres" autoComplete="email" className="w-full rounded-xl border border-white/10 bg-black/20 px-4 py-3 outline-none"/>{message&&<div className="rounded-xl bg-emerald-500/10 p-3 text-sm text-emerald-200">{message}</div>}{error&&<div className="rounded-xl bg-red-500/10 p-3 text-sm text-red-200">{error}</div>}<button disabled={busy} className="w-full rounded-xl bg-white px-4 py-3 font-bold text-slate-950 disabled:opacity-50">{busy?"Even geduld…":"Resetlink sturen →"}</button></form><a href="/account" className="mt-5 inline-block text-sm text-emerald-300">← Terug naar inloggen</a></div></div></main>;
}