"use client";
import { useState } from "react";
import DashboardNav from "../nav";
import AiAssistant from "@/components/ai-assistant";
import "../dashboard.css";
export default function Contact(){const [busy,setBusy]=useState(false),[status,setStatus]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const form=e.currentTarget;setBusy(true);setStatus("");const data=Object.fromEntries(new FormData(form));try{const r=await fetch("/api/contact",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)});const d=await r.json();if(!r.ok)throw new Error(d.error||"Verzenden mislukt.");setStatus("Bericht verzonden. We nemen zo snel mogelijk contact met je op.");form.reset()}catch(e){setStatus(e instanceof Error?e.message:"Verzenden mislukt.")}finally{setBusy(false)}}return <main className="rf-page"><div className="rf-shell"><header className="rf-header"><a href="/dashboard" className="rf-brand">RankFix <span>AI</span></a><a href="/dashboard" className="rf-back">← Dashboard</a></header><DashboardNav current={6}/><div className="rf-body"><div className="rf-heading"><h1>Contact</h1><p>Een vraag over je scan, account of fix? Stuur ons direct een bericht.</p></div><form className="rf-review-form" onSubmit={submit}><label>Naam<input name="name" required/></label><label>E-mail<input name="email" type="email" required/></label><label>Bedrijf <small>(optioneel)</small><input name="company"/></label><label>Bericht<textarea name="message" rows={6} required/></label><button className="rf-primary" disabled={busy}>{busy?"Verzenden…":"Bericht verzenden"}</button>{status&&<p className="rf-review-status" role="status">{status}</p>}</form></div></div><AiAssistant dashboard/></main>}